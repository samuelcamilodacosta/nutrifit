name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    environment: nutrifit

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: Install
      run: npm ci

    - name: Build
      run: npm run build
      env:
        VITE_GROQ_API_KEY: ${{ secrets.VITE_GROQ_API_KEY }}
        VITE_GROQ_MODEL: ${{ secrets.VITE_GROQ_MODEL }}

    - name: Test
      run: npm test

    - name: Coverage
      run: npm run test:coverage
